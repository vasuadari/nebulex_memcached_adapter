version: '3'
services:
  memcached:
    image: memcached:1.5-alpine
    ports:
      - "11211:11211"
      - "11211:11211/udp"
    network_mode: bridge

  test:
    build: .
    command: mix test
    network_mode: bridge
    links:
      - memcached
